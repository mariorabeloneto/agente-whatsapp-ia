# ── Configuração real do cliente (pode conter dados) ──
configs/config.yaml

# ── Segredos e credenciais ──
.env
*.env
agente/.env
google_calendar_tokens.json
*.pem
*.key

# ── Logs e dados locais ──
*.log
agente.log
__pycache__/
*.pyc

# ── Ambientes virtuais ──
venv/
.venv/
env/

# ── Editores / sistema ──
.vscode/
.idea/
.DS_Store

# ── Traces e resultados de evals (gerados) ──
traces/
evals/results.json
.pytest_cache/
